#include <stdio.h>

int main()
{
    int l, b;
    printf("Enter the length and breadth of Rectangle:");
    scanf("%d%d", &l, &b);

    // printing area
    printf("Area Of rectangle is : %d", l * b);
    return 0;
}